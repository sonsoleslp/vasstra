% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step2-sequences.R
\name{step2_sequences}
\alias{step2_sequences}
\title{Step 2: Turn States into Sequences}
\usage{
step2_sequences(
  data,
  id = NULL,
  time = NULL,
  state = NULL,
  state_order = NULL,
  state_colors = NULL,
  time_levels = NULL,
  missing = c("error", "explicit", "keep"),
  missing_label = "Missing"
)
}
\arguments{
\item{data}{A \code{vasstra_states} object or a data frame containing states.}

\item{id}{Subject identifier column. Inferred from \code{vasstra_states},
attached role metadata, or a common identifier name.}

\item{time}{Time or ordering column. Inferred from \code{vasstra_states},
attached role metadata, or a common time name.}

\item{state}{State column. Inferred from \code{vasstra_states}, or detected as
the single categorical non-role column of a plain data frame.}

\item{state_order}{Optional character vector giving the order the states
should appear in every plot. It must list exactly the observed states,
rearranged. Defaults to the state factor's levels, or alphabetical order
for a character state column.}

\item{state_colors}{Optional state palette stored on the result and reused
by every plot. A named vector (matched by state) or one colour per state
in state order. Inherited from a \code{vasstra_states} input when not given.}

\item{time_levels}{Explicit chronological values. Required for character or
unordered-factor time when \code{data} is a plain data frame.}

\item{missing}{Structural-gap policy: \code{"error"} (default), \code{"explicit"} to
create a state for missing subject-time cells, or \code{"keep"} to preserve
\code{NA} cells for advanced use.}

\item{missing_label}{Label used when \code{missing = "explicit"}.}
}
\value{
A \code{vasstra_sequences} object with \code{data} (wide state sequences),
\code{long_data}, \code{meta_data}, \code{distribution}, and \code{transitions}.
}
\description{
Aligns every subject on the same observed time axis and returns wide
sequences plus tidy long data, state distributions, and transitions.
}
\examples{
states <- data.frame(
  student = rep(1:3, each = 3),
  course = rep(1:3, 3),
  engagement = c("Low", "Average", "High",
                 "Average", "Average", "High",
                 "Low", "Low", "Average")
)
sequences <- step2_sequences(
  states,
  id = "student",
  time = "course",
  state = "engagement"
)
sequences
}
