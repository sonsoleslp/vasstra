% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-flow.R
\name{flow_plot}
\alias{flow_plot}
\alias{flow_plot.vasstra_sequences}
\alias{flow_plot.vasstra_trajectories}
\alias{flow_plot.vasstra}
\title{Plot State Flows Between Consecutive Time Points}
\usage{
flow_plot(x, ...)

\method{flow_plot}{vasstra_sequences}(
  x,
  type = c("alluvial", "individual"),
  colors = NULL,
  main = NULL,
  color_by = NULL,
  bundle = "auto",
  bundle_max = 50,
  ...
)

\method{flow_plot}{vasstra_trajectories}(
  x,
  type = c("alluvial", "individual"),
  colors = NULL,
  main = NULL,
  color_by = NULL,
  bundle = "auto",
  bundle_max = 50,
  group = NULL,
  ...
)

\method{flow_plot}{vasstra}(x, ...)
}
\arguments{
\item{x}{A \code{vasstra_sequences}, \code{vasstra_trajectories}, or \code{vasstra}
object.}

\item{...}{Additional arguments passed to \code{\link[cograph:plot_alluvial]{cograph::plot_alluvial()}} or
\code{\link[cograph:plot_trajectories]{cograph::plot_trajectories()}}.}

\item{type}{\code{"alluvial"} (default) for aggregated flow bands whose width
is the number of subjects making that move, or \code{"individual"} for one
line per subject.}

\item{colors}{Optional colors, one per state, in state order. Defaults to
the shared VaSStra palette.}

\item{main}{Optional plot title. A type-specific title is used by default.}

\item{color_by}{State that gives a flow its color: \code{"source"} (default for
\code{"alluvial"}) or \code{"destination"}. Individual lines also accept
\code{"first"} (the default, coloring each subject by the state they start
in) and \code{"last"}.}

\item{bundle}{Line bundling for \code{type = "individual"}. \code{"auto"} (default)
bundles only when subjects outnumber \code{bundle_max} lines, \code{FALSE} draws
every subject, and a number sets the subjects represented by one line.
Ignored by \code{type = "alluvial"}.}

\item{bundle_max}{Largest number of lines drawn before \code{"auto"} bundling
starts. Default 50.}

\item{group}{Optional trajectory label. Restricts the plot to the
subjects of one trajectory, which is how a single group's movement is
inspected; flow plots draw one panel and cannot be faceted.}
}
\value{
A \code{ggplot} object. Unlike the base-graphics \code{\link[=plot]{plot()}} methods,
which draw immediately and return their tidy data invisibly, this is
returned visibly so that it prints at the console.
}
\description{
Draws how subjects move between states from one time point to the next,
using \code{\link[cograph:plot_alluvial]{cograph::plot_alluvial()}} for aggregated bands and
\code{\link[cograph:plot_trajectories]{cograph::plot_trajectories()}} for individually tracked lines. Sequence
index, distribution, and heatmap views remain with \code{\link[=plot]{plot()}} and
Nestimate; \code{flow_plot()} answers the complementary question of \emph{where
movement goes}, which a sequence plot ordered by similarity cannot show.
}
\details{
State colors, state order, and time labels are taken from the fitted
object, so a flow plot is directly comparable with the sequence heatmap
and the state profiles.
}
\examples{
\donttest{
if (requireNamespace("cograph", quietly = TRUE)) {
  data("engagement", package = "VaSStra")
  fit <- vasstra(
    engagement,
    state_labels = c("Disengaged", "Average", "Active")
  )
  flow_plot(fit)
  flow_plot(fit, type = "individual")
  flow_plot(fit, group = "Trajectory 1")
}
}
}
\seealso{
\code{\link[=plot.vasstra_sequences]{plot.vasstra_sequences()}} for the Nestimate sequence views.
}
