% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{evaluate}
\alias{evaluate}
\alias{evaluate.vasstra_states}
\alias{evaluate.vasstra_trajectories}
\alias{evaluate.vasstra}
\title{Evaluate a Fitted Clustering}
\usage{
evaluate(x, ...)

\method{evaluate}{vasstra_states}(x, n_states = NULL, ...)

\method{evaluate}{vasstra_trajectories}(x, n_trajectories = NULL, ...)

\method{evaluate}{vasstra}(x, ...)
}
\arguments{
\item{x}{A \code{vasstra_states}, \code{vasstra_trajectories}, or complete
\code{vasstra} object.}

\item{...}{Method-specific arguments.}

\item{n_states}{Candidate state counts to compare. Defaults to the stored
automatic comparison when one exists, otherwise 2 through 6.}

\item{n_trajectories}{Candidate trajectory counts to compare. Defaults to
the stored automatic comparison when one exists, otherwise 2 through 6.}
}
\value{
A \code{vasstra_evaluation} object (or a \code{vasstra_evaluations} pair for
a complete fit). \code{as.data.frame()} returns the candidate comparison;
\code{summary()} returns the per-cluster quality table.
}
\description{
Compares the fitted number of states or trajectories against the
alternative counts for the same clustering method and reports per-cluster
quality. The result contains one tidy candidate row per compared count,
with the fitted and recommended solutions marked, and one tidy row per
fitted cluster with its size and mean silhouette width.
}
\examples{
set.seed(1)
data <- expand.grid(student = 1:12, course = 1:3)
level <- rep(c(2, 8, 16), length.out = nrow(data))
data$views <- level + rnorm(nrow(data), sd = 0.4)
data$duration <- level * 3 + rnorm(nrow(data), sd = 0.4)
states <- step1_states(data, n_states = 3)
evaluate(states)
}
