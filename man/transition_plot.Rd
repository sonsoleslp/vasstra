% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-transition.R
\name{transition_plot}
\alias{transition_plot}
\alias{transition_plot.vasstra_sequences}
\alias{transition_plot.vasstra_trajectories}
\alias{transition_plot.vasstra}
\title{Plot the State Transition Network}
\usage{
transition_plot(x, ...)

\method{transition_plot}{vasstra_sequences}(
  x,
  size = "InStrength",
  weights = c("probability", "count"),
  loops = FALSE,
  size_range = c(8, 18),
  sequences = FALSE,
  colors = NULL,
  main = NULL,
  ...
)

\method{transition_plot}{vasstra_trajectories}(
  x,
  size = "InStrength",
  weights = c("probability", "count"),
  loops = FALSE,
  size_range = c(8, 18),
  sequences = FALSE,
  colors = NULL,
  main = NULL,
  group = NULL,
  ...
)

\method{transition_plot}{vasstra}(x, ...)
}
\arguments{
\item{x}{A \code{vasstra_sequences}, \code{vasstra_trajectories}, or \code{vasstra}
object.}

\item{...}{Additional arguments passed to \code{\link[cograph:splot]{cograph::splot()}}, such as
\code{layout} or \code{threshold}.}

\item{size}{Centrality measure that sets node size, or \code{"none"} to draw
every node at one size. Default \code{"InStrength"}.}

\item{weights}{\code{"probability"} (default) uses row-normalized transition
probabilities from \code{\link[Nestimate:build_tna]{Nestimate::build_tna()}}; \code{"count"} uses raw
transition counts from \code{\link[Nestimate:build_ftna]{Nestimate::build_ftna()}}.}

\item{loops}{Include self-transitions in the computation. Default
\code{FALSE}, matching \code{\link[Nestimate:net_centrality]{Nestimate::net_centrality()}}.}

\item{size_range}{Smallest and largest node size. The measure is mapped
onto this range anchored at zero, so a state with half the in-strength
of the largest draws halfway up the range and sizes stay comparable
between plots.}

\item{sequences}{Draw the state sequences beside the network, the
conventional pairing in which the sequences show the raw data and the
network summarises its movement. \code{FALSE} (default) draws the network
alone; \code{TRUE} adds an index plot; \code{"index"}, \code{"heatmap"}, or
\code{"distribution"} choose the sequence view. Both panels are drawn on
one device, so a state has the same colour in each.}

\item{colors}{Optional colors, one per state, in state order. Defaults
to the shared VaSStra palette, so the network matches the sequence and
state plots. Colors are matched to states by name, so a trajectory
that never reaches a state still colours the rest correctly.}

\item{main}{Optional plot title.}

\item{group}{Optional trajectory label restricting the network to one
trajectory's subjects.}
}
\value{
A tidy data frame of the plotted centrality and node size, one
row per state, invisibly.
}
\description{
Draws states as nodes and transitions as directed edges, with node size
encoding a centrality of the transition network. The network comes from
\code{\link[Nestimate:build_tna]{Nestimate::build_tna()}} and the centralities from
\code{\link[Nestimate:net_centrality]{Nestimate::net_centrality()}}; \code{\link[cograph:splot]{cograph::splot()}} draws it, recognising
the Nestimate object and applying TNA styling, node labels, and
initial-probability donuts automatically.
}
\details{
Where \code{\link[=flow_plot]{flow_plot()}} shows movement resolved over time,
\code{transition_plot()} collapses every time step into one network and asks
which states attract movement overall.
}
\examples{
\donttest{
if (requireNamespace("cograph", quietly = TRUE)) {
  data("engagement", package = "VaSStra")
  fit <- vasstra(engagement, n_states = 3, n_trajectories = 3)
  transition_plot(fit)
  transition_plot(fit, weights = "count", size = "OutStrength")
  transition_plot(fit, sequences = TRUE)
}
}
}
\seealso{
\code{\link[=transition_centrality]{transition_centrality()}} for the numbers without a plot, and
\code{\link[=flow_plot]{flow_plot()}} for time-resolved movement.
}
