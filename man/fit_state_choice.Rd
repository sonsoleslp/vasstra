% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/choices.R
\name{fit_state_choice}
\alias{fit_state_choice}
\title{Fit One State Choice}
\usage{
fit_state_choice(
  choices,
  candidate_id = NULL,
  labels = NULL,
  state = "state",
  n_states = NULL,
  method = NULL,
  lpa_model = NULL
)
}
\arguments{
\item{choices}{A \code{vasstra_state_choices} object from \code{\link[=state_choices]{state_choices()}}.}

\item{candidate_id}{Optional explicit candidate number to fit.}

\item{labels}{Optional state labels ordered from low to high profile.}

\item{state}{Name of the state column created in the returned data.}

\item{n_states}{Optional state count used to select the candidate.}

\item{method}{Optional clustering method used to select the candidate.}

\item{lpa_model}{Optional LPA covariance model used to select the
candidate.}
}
\value{
A \code{vasstra_states} object from \code{\link[=step1_states]{step1_states()}} with the selected
candidate and complete comparison table recorded in \code{diagnostics}.
}
\description{
Fits a candidate from \code{\link[=state_choices]{state_choices()}}, selected by \code{candidate_id}, by
any combination of \code{n_states}, \code{method}, and \code{lpa_model}, or — when
nothing is specified — the recommended candidate.
}
\examples{
data <- expand.grid(student = 1:12, course = 1:3)
group <- rep(rep(1:3, each = 4), times = 3)
data$views <- group * 5 + data$course * 0.01
data$duration <- group * 10 - data$course * 0.01
choices <- state_choices(
  data, "student", "course", c("views", "duration"),
  n_states = 2:3, method = "kmeans"
)
fit_state_choice(choices)             # the recommended candidate
fit_state_choice(choices, n_states = 3)
}
