% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labels.R
\name{set_labels}
\alias{set_labels}
\alias{set_labels.vasstra_states}
\alias{set_labels.vasstra_trajectories}
\alias{set_labels.vasstra}
\title{Relabel Fitted States or Trajectories}
\usage{
set_labels(x, ...)

\method{set_labels}{vasstra_states}(x, labels, ...)

\method{set_labels}{vasstra_trajectories}(x, labels, ...)

\method{set_labels}{vasstra}(x, states = NULL, trajectories = NULL, ...)
}
\arguments{
\item{x}{A \code{vasstra_states}, \code{vasstra_trajectories}, or complete
\code{vasstra} object.}

\item{...}{Method-specific arguments.}

\item{labels}{New labels: a complete vector or named partial renames.}

\item{states}{New state labels for a complete fit (complete vector or
named renames). The rename is propagated in place through the fitted
states, sequences, trajectories, and description — including the
recorded positive and negative states — without changing any fitted
value.}

\item{trajectories}{New trajectory labels for a complete fit.}
}
\value{
The relabeled object, with the same class as \code{x}.
}
\description{
Renames the groups of an already-fitted object and propagates the new
labels through every derived table. Supply either a complete label
vector ordered from the first to the last group, or named renames such
as \code{c("State 1" = "Disengaged")} to change only some labels.
}
\examples{
set.seed(1)
data <- expand.grid(student = 1:12, course = 1:3)
level <- rep(c(2, 8, 16), length.out = nrow(data))
data$views <- level + rnorm(nrow(data), sd = 0.4)
data$duration <- level * 3 + rnorm(nrow(data), sd = 0.4)
states <- step1_states(data, n_states = 3)
states <- set_labels(states, c("Low", "Average", "High"))
summary(states)
}
