% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit-indices.R
\name{fit_indices}
\alias{fit_indices}
\alias{fit_indices.vasstra_states}
\alias{fit_indices.vasstra_trajectories}
\alias{fit_indices.vasstra}
\title{Tidy Fit Indices for a Fitted Clustering}
\usage{
fit_indices(x, ...)

\method{fit_indices}{vasstra_states}(x, compare = FALSE, n_states = NULL, ...)

\method{fit_indices}{vasstra_trajectories}(x, compare = FALSE, n_trajectories = NULL, ...)

\method{fit_indices}{vasstra}(x, step = c("states", "trajectories"), compare = FALSE, ...)
}
\arguments{
\item{x}{A \code{vasstra_states}, \code{vasstra_trajectories}, or complete
\code{vasstra} object.}

\item{...}{Method-specific arguments.}

\item{compare}{Return one row per compared candidate instead of only the
fitted solution.}

\item{n_states}{Candidate state counts used when \code{compare = TRUE}.
Defaults to the stored automatic comparison when one exists, otherwise
2 through 6.}

\item{n_trajectories}{Candidate trajectory counts used when
\code{compare = TRUE}. Defaults to the stored automatic comparison when one
exists, otherwise 2 through 6.}

\item{step}{Which clustering of a complete fit to summarize:
\code{"states"} (default) or \code{"trajectories"}.}
}
\value{
A \code{vasstra_fit_indices} data frame: one row for the fitted
solution, or one row per candidate (with \code{best} and \code{fitted} markers)
when \code{compare = TRUE}.
}
\description{
Returns the fit statistics of the selected clustering as one tidy row,
or one row per compared candidate with \code{compare = TRUE}. LPA solutions
report the complete information-criterion family (log-likelihood, AIC,
BIC, SABIC, CAIC, AWE, CLC, KIC, ICL — all on the conventional
lower-is-better scale), normalized entropy, and the smallest and largest
average posterior class probabilities. Hard clustering methods report
their own objectives (total within-cluster sum of squares, PAM
objective). Silhouette and group sizes are always included, and columns
that do not apply to the fitted method are dropped.
}
\examples{
set.seed(1)
data <- expand.grid(student = 1:12, course = 1:3)
level <- rep(c(2, 8, 16), length.out = nrow(data))
data$views <- level + rnorm(nrow(data), sd = 0.4)
data$duration <- level * 3 + rnorm(nrow(data), sd = 0.4)
states <- step1_states(data, n_states = 3)
fit_indices(states)
fit_indices(states, compare = TRUE)
}
