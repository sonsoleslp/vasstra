% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-transition.R
\name{transition_centrality}
\alias{transition_centrality}
\alias{transition_centrality.vasstra_sequences}
\alias{transition_centrality.vasstra_trajectories}
\alias{transition_centrality.vasstra}
\title{State Transition Network Centralities}
\usage{
transition_centrality(x, ...)

\method{transition_centrality}{vasstra_sequences}(
  x,
  measures = c("InStrength", "OutStrength"),
  weights = c("probability", "count"),
  loops = FALSE,
  ...
)

\method{transition_centrality}{vasstra_trajectories}(
  x,
  measures = c("InStrength", "OutStrength"),
  weights = c("probability", "count"),
  loops = FALSE,
  group = NULL,
  ...
)

\method{transition_centrality}{vasstra}(x, ...)
}
\arguments{
\item{x}{A \code{vasstra_sequences}, \code{vasstra_trajectories}, or \code{vasstra}
object.}

\item{...}{Not used.}

\item{measures}{Centrality measures to compute, passed to
\code{\link[Nestimate:net_centrality]{Nestimate::net_centrality()}}. Defaults to \code{c("InStrength", "OutStrength")}; \code{"all"} returns every built-in measure.}

\item{weights}{\code{"probability"} (default) uses row-normalized transition
probabilities from \code{\link[Nestimate:build_tna]{Nestimate::build_tna()}}; \code{"count"} uses raw
transition counts from \code{\link[Nestimate:build_ftna]{Nestimate::build_ftna()}}.}

\item{loops}{Include self-transitions in the computation. Default
\code{FALSE}, matching \code{\link[Nestimate:net_centrality]{Nestimate::net_centrality()}}.}

\item{group}{Optional trajectory label restricting the network to one
trajectory's subjects.}
}
\value{
A tidy data frame with one row per state and one column per
requested measure.
}
\description{
Tidy centralities of the state transition network, one row per state.
The network is built by \code{\link[Nestimate:build_tna]{Nestimate::build_tna()}} and the measures come
from \code{\link[Nestimate:net_centrality]{Nestimate::net_centrality()}}, so they match \code{tna::centralities()}.
}
\details{
In-strength is the total incoming transition weight, so the state with
the largest in-strength is the one the cohort most often moves \emph{into}.
Self-transitions are excluded by default: with \code{loops = TRUE} a
persistent state scores highly merely because its members stay put,
which is a different claim from attracting movement.
}
\examples{
data("engagement", package = "VaSSTra")
fit <- vasstra(engagement, n_states = 3, n_trajectories = 3)
transition_centrality(fit)
transition_centrality(fit, weights = "count")
}
\seealso{
\code{\link[=transition_plot]{transition_plot()}} to draw the network.
}
