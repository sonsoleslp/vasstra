% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.vasstra_sequences}
\alias{plot.vasstra_sequences}
\title{Plot VaSSTra State Sequences with Nestimate}
\usage{
\method{plot}{vasstra_sequences}(
  x,
  type = c("heatmap", "distribution", "index"),
  colors = NULL,
  main = NULL,
  sort = "lcs",
  na = x$diagnostics$n_missing > 0L,
  ...
)
}
\arguments{
\item{x}{A \code{vasstra_sequences} object.}

\item{type}{One of \code{"heatmap"} (default), \code{"distribution"}, or
\code{"index"}. The heatmap shows every aligned sequence at full
resolution and is the most informative single view of the complete
cohort.}

\item{colors}{Optional colors, one per state, passed to Nestimate as
\code{state_colors}.}

\item{main}{Plot title. A type-specific title is used by default.}

\item{sort}{Sequence ordering passed to \code{\link[Nestimate:sequence_plot]{Nestimate::sequence_plot()}}.}

\item{na}{Show missing cells as a separate distribution band. By default,
this is \code{TRUE} only when the sequence data contain \code{NA}.}

\item{...}{Additional arguments passed to \code{\link[Nestimate:sequence_plot]{Nestimate::sequence_plot()}}.}
}
\value{
The value returned by \code{\link[Nestimate:sequence_plot]{Nestimate::sequence_plot()}}.
}
\description{
Delegates sequence index, distribution, and heatmap rendering to
\code{\link[Nestimate:sequence_plot]{Nestimate::sequence_plot()}}.
}
\examples{
data("engagement", package = "VaSSTra")
fit <- vasstra(engagement, n_states = 3, n_trajectories = 3)
plot(fit$sequences)
plot(fit$sequences, type = "distribution")
}
\seealso{
\code{\link[=flow_plot]{flow_plot()}} for alluvial and individual flow views.
}
