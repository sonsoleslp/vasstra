% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/choices.R
\name{state_choices}
\alias{state_choices}
\title{Compare Choices for the Number and Method of States}
\usage{
state_choices(
  data,
  id = NULL,
  time = NULL,
  variables = NULL,
  n_states = 2:6,
  method = c("lpa", "kmeans", "pam", "ward.D2"),
  lpa_model = "EEI",
  lpa_criterion = c("bic", "aic", "silhouette", "icl_native"),
  standardize = NULL,
  missing = NULL,
  time_levels = NULL,
  n_start = 25L,
  seed = 123L,
  minimum_size = 2L,
  minimum_proportion = 0,
  maximum_size_ratio = Inf
)
}
\arguments{
\item{data}{A data frame with one row per subject and time point.}

\item{id}{Name of the subject identifier column. May be omitted when the
data carry VaSSTra role metadata.}

\item{time}{Name of the time or ordering column. May be omitted when the
data carry VaSSTra role metadata.}

\item{variables}{Character vector naming numeric state indicators. May be
omitted when the data carry VaSSTra role metadata.}

\item{n_states}{Whole-number candidate state counts.}

\item{method}{State-clustering methods to compare. See \code{\link[=step1_states]{step1_states()}}.}

\item{lpa_model}{Optional mclust covariance models evaluated when
\code{method} includes \code{"lpa"}. \code{"EEI"} corresponds to tidyLPA model 1.}

\item{lpa_criterion}{Criterion used only to mark LPA recommendations:
conventional \code{"bic"} (default) or \code{"aic"} (smaller is better),
\code{"silhouette"} (larger is better), or native mclust \code{"icl_native"}
(larger is better).}

\item{standardize, missing, time_levels, n_start, seed}{Passed to
\code{\link[=step1_states]{step1_states()}}.}

\item{minimum_size}{Minimum acceptable number of observations in every
state.}

\item{minimum_proportion}{Minimum acceptable proportion in every state.}

\item{maximum_size_ratio}{Maximum acceptable largest-to-smallest state-size
ratio.}
}
\value{
A \code{vasstra_state_choices} object. \code{as.data.frame()} returns one
tidy row per candidate; \verb{$recommendations} contains one transparent
recommendation per method and LPA model.
}
\description{
Fits a tidy grid of state solutions. No solution is silently selected.
Hard-clustering recommendations maximize silhouette within each method;
LPA recommendations use the explicit \code{lpa_criterion}, subject to the
requested size constraints. Use \code{\link[=fit_state_choice]{fit_state_choice()}} with a \code{candidate_id}
after inspecting the table.
}
\examples{
data <- expand.grid(student = 1:12, course = 1:3)
group <- rep(rep(1:3, each = 4), times = 3)
data$views <- group * 5 + data$course * 0.01
data$duration <- group * 10 - data$course * 0.01
choices <- state_choices(
  data,
  id = "student",
  time = "course",
  variables = c("views", "duration"),
  n_states = 2:4,
  method = c("kmeans", "pam")
)
choices
states <- fit_state_choice(
  choices,
  candidate_id = choices$recommendations$candidate_id[[1L]]
)
}
