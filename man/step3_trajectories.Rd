% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step3-trajectories.R
\name{step3_trajectories}
\alias{step3_trajectories}
\title{Step 3: Turn Sequences into Trajectories}
\usage{
step3_trajectories(
  data,
  n_trajectories = "auto",
  dissimilarity = c("hamming", "osa", "lv", "dl", "lcs", "qgram", "cosine", "jaccard",
    "jw"),
  method = c("pam", "ward.D2", "ward.D", "complete", "average", "single", "mcquitty",
    "median", "centroid"),
  backend = c("Nestimate", "base"),
  labels = NULL,
  seed = 123L
)
}
\arguments{
\item{data}{A \code{vasstra_sequences} object.}

\item{n_trajectories}{Number of trajectory groups. One number fits
exactly that count, several numbers (for example \code{2:4}) compare those
candidates with \code{\link[=trajectory_choices]{trajectory_choices()}} and fit the recommended count,
and \code{"auto"} (default) compares 2 through 6 — or simply matches
\code{labels} when labels are supplied. Automatic comparison never selects
a solution whose smallest group holds under 5 percent of the
sequences. Compared candidates are kept in \code{diagnostics$selection}
and every automated choice is reported with a message.}

\item{dissimilarity}{One of \code{"hamming"}, \code{"osa"}, \code{"lv"}, \code{"dl"}, \code{"lcs"},
\code{"qgram"}, \code{"cosine"}, \code{"jaccard"}, or \code{"jw"}.}

\item{method}{One of \code{"pam"}, \code{"ward.D2"}, \code{"ward.D"}, \code{"complete"},
\code{"average"}, \code{"single"}, \code{"mcquitty"}, \code{"median"}, or \code{"centroid"}.}

\item{backend}{\code{"Nestimate"} (default) uses
\code{\link[Nestimate:build_clusters]{Nestimate::build_clusters()}}. Use \code{"base"} for the small internal
equivalence backend, which supports only Hamming/LCS distances.}

\item{labels}{Optional unique trajectory labels.}

\item{seed}{Reproducible random seed passed to the clustering backend.}
}
\value{
A \code{vasstra_trajectories} object with tidy \code{membership}, cluster
sizes, silhouette, and the complete distance matrix.
}
\description{
Clusters aligned state sequences with Nestimate sequence distances and
clustering methods. The default Hamming plus PAM combination follows the
Nestimate and Carm sequence-clustering interface. Set
\code{dissimilarity = "lcs"} and \code{method = "ward.D2"} to follow the worked
VaSSTra chapter.
}
\examples{
sequences <- step2_sequences(
  data.frame(
    id = rep(1:6, each = 3),
    time = rep(1:3, 6),
    state = c(
      "A", "A", "A", "A", "A", "B",
      "B", "B", "B", "B", "B", "A",
      "C", "C", "C", "C", "C", "B"
    )
  ),
  id = "id",
  time = "time",
  state = "state"
)
trajectories <- step3_trajectories(sequences, n_trajectories = 3)
trajectories
}
