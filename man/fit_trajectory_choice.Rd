% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/choices.R
\name{fit_trajectory_choice}
\alias{fit_trajectory_choice}
\title{Fit One Trajectory Choice}
\usage{
fit_trajectory_choice(
  choices,
  candidate_id = NULL,
  labels = NULL,
  n_trajectories = NULL,
  dissimilarity = NULL,
  method = NULL
)
}
\arguments{
\item{choices}{A \code{vasstra_trajectory_choices} object.}

\item{candidate_id}{Optional explicit candidate number to fit.}

\item{labels}{Optional trajectory labels.}

\item{n_trajectories}{Optional trajectory count used to select the
candidate.}

\item{dissimilarity}{Optional sequence distance used to select the
candidate.}

\item{method}{Optional clustering method used to select the candidate.}
}
\value{
A \code{vasstra_trajectories} object with the selected candidate and
complete comparison table recorded in \code{diagnostics}.
}
\description{
Fits a candidate from \code{\link[=trajectory_choices]{trajectory_choices()}}, selected by
\code{candidate_id}, by any combination of \code{n_trajectories},
\code{dissimilarity}, and \code{method}, or — when nothing is specified — the
recommended candidate.
}
\examples{
sequences <- step2_sequences(
  data.frame(
    id = rep(1:6, each = 3),
    time = rep(1:3, 6),
    state = rep(c("A", "A", "A", "B", "B", "B"), each = 3)
  ),
  "id", "time", "state"
)
choices <- trajectory_choices(
  sequences, n_trajectories = 2, dissimilarity = "hamming",
  method = "pam"
)
fit_trajectory_choice(choices)
}
