% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step1-states.R
\name{step1_states}
\alias{step1_states}
\title{Step 1: Turn Variables into States}
\usage{
step1_states(
  data,
  id = NULL,
  time = NULL,
  variables = NULL,
  n_states = "auto",
  labels = NULL,
  state = "state",
  standardize = NULL,
  missing = NULL,
  time_levels = NULL,
  n_start = 25L,
  seed = 123L,
  method = c("lpa", "kmeans", "pam", "ward.D2", "ward.D", "complete", "average",
    "single", "mcquitty", "median", "centroid"),
  lpa_model = "EEI"
)
}
\arguments{
\item{data}{A data frame with one row per subject and time point.}

\item{id}{Name of the subject identifier column. May be omitted when the
data carry VaSStra role metadata.}

\item{time}{Name of the time or ordering column. May be omitted when the
data carry VaSStra role metadata.}

\item{variables}{Character vector naming numeric state indicators. May be
omitted when the data carry VaSStra role metadata.}

\item{n_states}{Number of states to estimate. One number fits exactly
that count, several numbers (for example \code{2:4}) compare those
candidates with \code{\link[=state_choices]{state_choices()}} and fit the recommended count, and
\code{"auto"} (default) compares 2 through 6 — or simply matches \code{labels}
when labels are supplied. Automatic comparison never selects a
solution whose smallest state holds under 5 percent of the
observations. Compared candidates are kept in
\code{diagnostics$selection} and every automated choice is reported with a
message.}

\item{labels}{Optional unique labels, ordered from the lowest to the highest
average standardized profile.}

\item{state}{Name of the state column created in the returned data.}

\item{standardize}{One of \code{"time"} (default for ordinary data),
\code{"global"}, or \code{"none"}. Analysis-ready package data may provide its
own default through metadata.}

\item{missing}{One of \code{"error"} (default for ordinary data) or
\code{"median"}. Analysis-ready package data may provide its own default
through metadata. Median imputation
is performed within time point, with the global variable median as a
fallback for an entirely missing time point.}

\item{time_levels}{Explicit chronological time values. Required for
character or unordered-factor time columns.}

\item{n_start}{Number of random k-means starts.}

\item{seed}{Reproducible random seed.}

\item{method}{State-clustering method. Choose \code{"lpa"} (default,
Gaussian-mixture latent profile analysis via mclust), \code{"kmeans"},
\code{"pam"}, or a hierarchical method: \code{"ward.D2"}, \code{"ward.D"},
\code{"complete"}, \code{"average"}, \code{"single"}, \code{"mcquitty"}, \code{"median"}, or
\code{"centroid"}.}

\item{lpa_model}{Covariance model used only by \code{method = "lpa"}. The
default \code{"EEI"} corresponds to tidyLPA model 1: equal variances and zero
covariances.}
}
\value{
A \code{vasstra_states} object. Its \code{data} element is the original data
with a factor state column; \code{profiles} is a tidy state-by-indicator table.
}
\description{
Standardizes numeric indicators and clusters each subject-time observation.
The default method is Gaussian-mixture latent profile analysis (LPA);
reproducible k-means, PAM, and hierarchical clustering are available
through \code{method}. Clusters are ordered by their average standardized
indicator level, then optionally given user-supplied labels.
}
\examples{
set.seed(1)
data <- expand.grid(student = 1:12, course = 1:3)
level <- rep(c(2, 8, 16), length.out = nrow(data))
data$views <- level + rnorm(nrow(data), sd = 0.4)
data$duration <- level * 3 + rnorm(nrow(data), sd = 0.4)
states <- step1_states(
  data,
  id = "student",
  time = "course",
  variables = c("views", "duration"),
  labels = c("Low", "Average", "High")
)
states
}
