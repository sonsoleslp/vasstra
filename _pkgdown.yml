# Served from pak.dynasite.org — the gh-pages branch built by
# .github/workflows/pkgdown.yaml is mirrored there by the dynasite-infra cron
# (see dynasite-infra/PACKAGES.md).
url: https://pak.dynasite.org/VaSStra

template:
  bootstrap: 5
  bslib:
    primary: "#1a5276"

navbar:
  structure:
    left:  [intro, reference, articles, news]
    right: [search, github]
